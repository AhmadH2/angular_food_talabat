export class Restaurant {

    constructor(public id: number, public name: string, public city: string,
        public street: string, public lat: string,
        public lng: string, public phone: string, public image: string, public rating: number) { }
}
