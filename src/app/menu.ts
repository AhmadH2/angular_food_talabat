export class Menu {
    public quantity: number = 0;
    constructor(public id: number, public rest_id: number, public name: string,
        public descr: string, public price: number, public image: string, public rating:number) { }
}
